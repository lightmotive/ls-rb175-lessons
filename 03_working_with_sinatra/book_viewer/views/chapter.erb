<h2 class="content-subhead"><%= @content_subhead %></h2>

<% plain_text_paragraph_enum(@chapter_data_string).each_with_index do |paragraph, idx| %>
<p id='paragraph<%= idx + 1 %>'><%= highlight_query(paragraph, @query) %></p>
<% end %>